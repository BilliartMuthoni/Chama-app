import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // turbopack: {
  //   root: __dirname,
  // },
  // Disabled temporarily due to LightningCSS native module issues
  // Ignore workspace root warning (pnpm monorepo setup)
};

export default nextConfig;
